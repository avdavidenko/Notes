Flask

Запрос:
GET: http://(протокол) server.ru(хост, "адрес дома") :8888(порт, "номер квартиры") /page/(адрес страницы, раздел сайта, путь) ?a=1&b=2(параметры)
ya.ru: 80

Ответ:
- HTML
- шаблон - HTML
- редирект
